# Preset M3 — Bimolecular association
# Mechanism
A + B -> C ; kf = 0.2

# Initials
[A] = 1.0
[B] = 1.0
[C] = 0.0

# Algebra
let extent = [C]
let mass_A = [A] + [C]     # if stoich is A + B -> C, each C consumes one A
let mass_B = [B] + [C]
